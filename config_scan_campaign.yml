instruction_set: aarch64
# Branch-target non-interference: seal the indirect call's target as canonical/non-canonical and
# (mis)aligned, and compare the sealed variants on hardware.
instruction_categories:
  - BASE-ARITH
  - BASE-LOGICAL
  - BASE-SHIFT
  - BASE-BITFIELD
  - BASE-CONDSEL
  - BASE-BRANCH
  - BASE-MEM-LOAD
  - BASE-MEM-STORE
program_size: 24
max_functions_per_test_case: 8
max_calls_per_function: 1
max_successors_per_bb: 2
min_successors_per_bb: 1
fuzzer: non-interference
input_generator: aarch64-nzcv
enable_branch_target_sealing: true       # canonicality + alignment of the branch target
branch_target_seal_misalign: true
enable_speculative_store_bypass: true    # v4/SSB window
contract_execution_clause:
  - cond
  - bpas

# TRIAGE INLINE: priming runs the input-dependence swap-test on every violation during fuzzing,
enable_priming: true

# auto-detect the CROSS-INPUT leftover (keep only predecessor-driven; drop per-input)
prime_predecessor_debug: true
enable_leftover_scan: true
enable_leftover_scan: true
prime_predecessor_debug: false
triage_reps: 60
inputs_per_class: 2
executor_pinned_core: -1
